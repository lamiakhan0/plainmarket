import type { NewsItem } from "@/types";

/**
 * Mock recent-news items, tagged by company symbol. Populated in a later step.
 * Illustrative only. Read via the data provider.
 */
export const news: NewsItem[] = [];
