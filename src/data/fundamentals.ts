import type { Fundamentals } from "@/types";

/**
 * Mock financial snapshots, one per company. Populated in a later step.
 * Illustrative only — not real market data. Read via the data provider.
 */
export const fundamentals: Fundamentals[] = [];
